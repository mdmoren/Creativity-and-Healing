import React from "react";

const GenderedViolence = () => {
  return (
    <div className="flex flex-col items-center justify-center w-screen">
      <div
        name="gv1"
        className="flex items-center justify-center w-full h-screen"
      >
        what it is
      </div>
      <div
        name="gv2"
        className="flex items-center justify-center w-full h-screen"
      >
        where it comes from
      </div>
      <div
        name="gv3"
        className="flex items-center justify-center w-full h-screen"
      >
        who it affects
      </div>
    </div>
  );
};

export default GenderedViolence;
